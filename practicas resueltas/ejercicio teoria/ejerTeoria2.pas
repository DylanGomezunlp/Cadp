program porValor;
var
	x:integer;
procedure uno (var num:integer; j:char);
begin
	if (x=7) then
	    x:= x+1;
	write(x);
	writeln(j);
end;
begin
	uno (x, 'p');
end.
//si se puede porque el tipo de dato ingresado en la invocacion del modulo coincide con el tipo requerido en esa posicion del modulo
